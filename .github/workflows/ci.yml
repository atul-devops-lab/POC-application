name: CI

on:
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Placeholder test step
        run: |
          echo "Running placeholder checks..."
          echo "No real app code yet — this will be replaced with real tests soon."
          exit 0
